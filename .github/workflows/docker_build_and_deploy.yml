name: Docker Build and Deploy

on:
  pull_request:
    types: [closed]

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        with:
          version: v0.7.0

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew
        shell: bash

      - name: Docker Login
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image and push to Docker Hub
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: qkrwodbsfjq/photolancer:latest
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

      - name: SSH to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}

        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          DOCKER_IMAGE_NAME: qkrwodbsfjq/photolancer:latest

      - name: Pull image from Docker Hub
        run: docker pull qkrwodbsfjq/photolancer:latest

      - name: Run Docker container with environment variables
        run: docker run --name photolancer -d -p 80:8080 qkrwodbsfjq/photolancer:latest

        env:
          DB_URL: jdbc:mysql://3.39.28.127:3306/photolancer
          DB_USERNAME: root
          DB_PASSWORD: 346464
          ACCESS_KEY: AKIAU7DZEUMKGEUDN7NT
          SECRET_KEY: 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

      - name: Check Docker container logs
        run: docker logs photolancer